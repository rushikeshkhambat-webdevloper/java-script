// we are learning abut key words in the js this key words are used to create a varibales in js 
const accId = "245678"// const means we cant not change the value in the accId 
let accState;// if we not assign any value in this it will give us an output of undefined
let   accHolder = "rushi"// let keyword allows us to modify the value in the variable 
var   accCity =" csnv "// here var is nothing but keyword which also allow us to make changes in value but it is old way we did not used it now we let instead of var 
// to print every thin in we will used table 
accHolder = " abhi"
arrCity = " bhopal"
console.table([accId,accCity,accHolder,accState]); // we will get our output in the form of tabel 
